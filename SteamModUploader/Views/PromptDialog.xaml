<Window x:Class="SteamModUploader.PromptDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="输入" Width="440" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#F3F4F8" ShowInTaskbar="False">
    <StackPanel Margin="18">
        <TextBlock x:Name="PromptText" Foreground="#2A475E" FontSize="13" TextWrapping="Wrap"/>
        <TextBox x:Name="InputBox" Margin="0,10,0,0" Padding="6,6" FontSize="14"
                 BorderBrush="#D4D9E3" BorderThickness="1"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="确定" Width="86" Margin="0,0,10,0" Padding="0,6"
                    Click="Ok_Click" IsDefault="True"/>
            <Button Content="取消" Width="86" Padding="0,6"
                    Click="Cancel_Click" IsCancel="True"/>
        </StackPanel>
    </StackPanel>
</Window>
